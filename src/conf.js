const environmentVars = {
    google_oauth_2_client_id : import.meta.env.VITE_GOOGLE_OAUTH_CLIENT_ID,
    address_of_the_server: import.meta.env.VITE_ADDRESS_OF_THE_SERVER,
    api_version: import.meta.env.VITE_API_VERSION,
};

export default environmentVars;
